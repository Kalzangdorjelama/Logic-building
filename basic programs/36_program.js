// Problem Statement - 36

// Q.NO: Write a program that checks if a given string of parentheses is balanced and well-formed parentheses is balanced and well-formed.
// Input: A string
// Output:
// -> True if parentheses is balanced
// -> False if parentheses is not balanced

// What are parentheses
// -> [] -> Square bracket
// -> {} -> Curly bracket
// -> () -> Circular bracket

// What is a balanced parentheses
// -> Every opening parenthesis has a corresponding closing parenthesis
// -> Parenthesis must be closed in the correct order

// Examples:
// -> [{()}] -> Balanced
// -> {() -> Not balanced / Unbalanced
// -> [{(})] -> Not balanced / Unbalanced

console.log(Object.keys(obj));
console.log(Object.values(obj));